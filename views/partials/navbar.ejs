<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Build <span class="logo-color">Guild</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="/pages">Builds</a>
                </li>
                <% if(currentUser) {%>
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="/pages/new">New</a>
                </li>
                <% } %>
            </ul>
            <div class="navbar-nav ml-auto">
                <% if(!currentUser) {%>
                <a class="nav-link btn-primary btn-light" href="/login">Login</a>
                <a class="nav-link btn-primary btn-light" href="/register">Register</a>
                <% } else {%>
                    <li class="nav-item">
                        <a class="nav-link muted" data-toggle="tab" href="#"><%=currentUser.username%></a>
                    </li>
                <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
